[package]
name = "verlet"
authors = ["Logan Gatlin logan@gatlintc.com"]
version = "0.1.0"
edition = "2021"
license = "MIT"

[features]
serde = ["dep:serde"]

[dependencies]
serde = {version = "1.0", features = ["derive"], optional = true}

